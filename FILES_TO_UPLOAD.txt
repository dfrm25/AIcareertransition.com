===========================================
DEPRECATED — USE GIT DEPLOY INSTEAD
===========================================

Manual FTP file lists are no longer maintained here.

Single deployment flow:
  Cursor → git push origin main → cPanel → Update from Remote → Deploy HEAD Commit

See:
  - DEPLOYMENT.md     (full workflow + production deployment log)
  - DEPLOY_MANIFEST.txt (files that should appear in public_html)
  - .cpanel.yml       (what the server runs on deploy)

===========================================
